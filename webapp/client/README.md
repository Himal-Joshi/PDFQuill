# PDFQuill Web Client

React and Vite frontend for PDFQuill.

## Development

```bash
npm install
npm run dev
```

The client expects the API at `http://localhost:4000` unless `VITE_API_BASE_URL` is set.

## Build

```bash
npm run build
```
